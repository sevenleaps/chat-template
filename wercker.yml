box:
  id: nodesource/trusty:4.4.2
  env:
    NODE_ENV: test

build:
  steps:
    - script:
        name: Updating NPM
        code: |
          npm -v
          npm install npm -g
          npm -v
    - npm-install
    - npm-test
    - script:
        name: build code
        code: |
          npm run build

  after-steps:
    - slack-notifier:
            url: $SLACK_WEBHOOK_URL
            username: wercker
