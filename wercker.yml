box: nodesource/trusty:4.4.2
  env:
    NODE_ENV: test

build:
  steps:
    - npm-install
    - npm-test
    - script:
        name: build code
        code: |
          npm run build

  after-steps:
    - slack-notifier:
            url: $SLACK_WEBHOOK_URL
            username: wercker
